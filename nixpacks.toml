[phases.setup]
cmds = [
  # Buat direktori /etc/nginx/ jika belum ada
  'mkdir -p /etc/nginx',

  # Salin file php.ini dan modifikasi
  'cp /nix/store/7qvqslx4fpz7qwgbd9091lgcs708b4gg-php-with-extensions-8.1.29/lib/php.ini /tmp/php.ini',
  'sed -i "s/^upload_max_filesize = .*/upload_max_filesize = 10M/" /tmp/php.ini',
  'sed -i "s/^post_max_size = .*/post_max_size = 10M/" /tmp/php.ini',
  'cp /tmp/php.ini /nix/store/7qvqslx4fpz7qwgbd9091lgcs708b4gg-php-with-extensions-8.1.29/lib/php.ini',

  # Salin file nginx.conf kustom
  'cp ./nginx.conf /etc/nginx/nginx.conf'
]